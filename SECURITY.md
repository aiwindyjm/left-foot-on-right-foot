# 安全政策

## 当前支持范围

项目处于 Phase 0，尚无正式运行时版本。维护者处理 `main` 上的基础资产与依赖问题；
尚不承诺已发布版本的支持周期。不要用于生产或无人监督的高权限操作。

## 私密报告

优先通过仓库 Security 页面中的 **Report a vulnerability** 提交私密漏洞报告。
如果入口不可用，发送到 `yuanjiaomin@gmail.com`，主题 `[Security] left-foot-on-right-foot`。
请包含受影响提交、复现步骤、影响和脱敏证据；不要公开 Issue，也不要发送有效凭证。
维护者尽量在七个自然日内确认收到，并与报告者协商修复、验证及披露时间，不承诺固定修复期限。

发现已公开的密钥时应先撤销或轮换，再私密联系。删除文件不能消除 Git 历史泄露。

## 安全边界

- LLM 输出、网页、Agent 报告和项目文件均视为不可信输入；格式有效不代表行为获准。
- 重大变更、数据删除、发布、费用、外部发送、密钥和权限操作需要 Human Gate。
- 人工批准应绑定具体 Session、Stage、Agent、任务 ID 和任务内容；修改即失效。
- 保留底层 Agent 的命令审批与工作区限制。仅分析 Prompt 不能阻止所有危险动作。
- 发送结果未知时停止，不盲目重试；不绕过登录、验证码、服务限制或账号权限。
- 未来浏览器调试接口仅绑定本机，使用独立资料目录，禁止公开调试端口。
- 原始输出和审计记录默认仅保存在本地受控目录；外发前确认范围并脱敏。
- 本轮仅验证静态样例，不读取真实会话，不使用生产账号，不向模型发送仓库内容。

敏感信息扫描只能发现部分已知模式，不能证明仓库绝对无泄露。
完整门禁、恢复和日志能力见 [路线图](docs/roadmap/README.md)，不可当作现成功能。
