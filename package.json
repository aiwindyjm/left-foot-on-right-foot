{
  "name": "left-foot-on-right-foot",
  "version": "0.0.0",
  "private": true,
  "description": "Project foundation for an experimental AI-to-agent workflow bridge.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=24 <25",
    "npm": ">=11 <12"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate": "npm test && node scripts/validate.mjs",
    "check:secrets": "node scripts/check-secrets.mjs"
  },
  "devDependencies": {
    "ajv": "8.20.0",
    "jsonc-parser": "3.3.1",
    "remark-parse": "11.0.0",
    "unified": "11.0.5"
  }
}
