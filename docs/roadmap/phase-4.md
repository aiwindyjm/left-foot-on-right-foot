# Phase 4: Multi-Agent Workflow

目标版本：v0.4.0。状态：未实现。

入口：多个 Adapter 的单会话行为稳定，人类批准多 Agent 权限与冲突模型。
范围：有限工作流、路由、任务队列与交接，定义任务所有权和并发边界。

验收：研究、编码、审查等角色能够按明确工作流交接；
不会同时覆盖同一工作区修改；失败、限额和人工门禁能够中断后续派发；
可追踪每项任务的来源、授权、执行者和验证证据。
不做：无限自组织群聊、自动产生无界任务或全功能 Agent OS。
出口：工作流及故障场景经过测试和人类验收，然后停止。
