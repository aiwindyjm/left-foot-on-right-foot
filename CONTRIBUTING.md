# 贡献指南

欢迎参与。当前落实桌面计划文档基线，有旧隔离评估工具但没有桌面应用或可运行闭环；可贡献文档、协议反例、工具测试与接口研究。
先读 [AGENTS.md](AGENTS.md)、[路线图](docs/roadmap/README.md)和[开发说明](docs/development.md)。

## 提一个能被认真审查的改动

1. Bug 使用问题模板，写清环境、重现步骤、预期与实际行为，移除隐私。
2. 新能力或架构变更先开提案 Issue，说明所属阶段、边界、风险和验收标准。
3. Fork 仓库，从 `main` 创建聚焦分支，如 `docs/protocol-errors`；不直接推维护分支。
4. 使用 Node.js 24，执行 `npm ci`；只修改本任务必要文件。
5. 运行 `npm test`、`npm run validate` 和 `git diff --check`，在 PR 中写明结果。
6. 提交 PR，关联 Issue、解释需求依据和兼容性；维护者审查后合并。

每个 PR 只解决一件清楚的事。协议改动必须附有效/无效例子；
未来监督行为改动需有固定证据样例和判断质量评估，不能只用 JSON 格式或转发成功证明正确；
未来 Adapter 改动必须附契约测试，真实账号测试只能手动执行并脱敏。
桌面/原生贡献须遵守 [接入验收](docs/desktop-integration.md)，区分共享核心、产品会话和系统控制；
Windows通过不代表Mac支持，SDK安装和真实会话操作不可当作默认测试步骤。
纯文档改动也应通过链接与基础验证。不得把未来能力写成已实现。

## 约定

- 使用 `feat`、`fix`、`docs`、`test`、`chore`、`ci`、`refactor` 类型的提交说明。
- 文档以中文为主，保留必要英文接口名称；代码、路径、协议枚举使用英文。
- 不提交依赖目录、密钥、Cookie、浏览器资料或个人配置；提交锁文件。
- AI 辅助贡献与人工贡献接受同样审查。PR 说明 AI 参与范围，作者对正确性负责。
- 架构与协议重大变更需要维护者明确批准，不以 CI 通过代替人工判断。
- 提交你有权贡献的内容；贡献按本仓库 [MIT](LICENSE) 分发，不要求单独 CLA。
- 维护者为 `@aiwindyjm`；分歧先在 Issue 中讨论，最终范围和合并由维护者决定。

发布、兼容性和版本规则见 [开发与发布](docs/development.md)。
私密漏洞请按 [安全政策](SECURITY.md)报告；社区行为请遵守 [行为准则](CODE_OF_CONDUCT.md)。

阶段完成后，贡献者应在变更说明中注明是否达到下一个 Release 门槛。普通 PR 不创建 Release；
Release 由维护者在验收提交上统一创建，预发布版本必须明确标注未完成能力和测试边界。
